# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
#
# Two ways this workflow runs:
#
#  1. As a reusable workflow chained off `build.yml`. After every variant
#     in the build matrix successfully pushes its container, `build.yml`
#     calls this with `workflow_call` so the ISO build sees the freshly
#     pushed `:unstable` / `:testing` digest. This eliminates the prior
#     race where build-iso pulled `:unstable` while build.yml was still
#     pushing it.
#
#  2. Directly on installer-only changes (e.g. tweaks under installer/
#     that do not invalidate the container image itself). In that case
#     the trigger fires standalone — no container rebuild needed.
name: Build Live ISO

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches: [unstable, testing]
    paths:
      - installer/**
      - .github/workflows/build-iso.yml
  pull_request:
    branches: [unstable, testing]
    paths:
      - installer/**
      - .github/workflows/build-iso.yml

env:
  REPO_OWNER: ${{ github.repository_owner }}

permissions: {}

jobs:
  build-iso:
    name: ISO ${{ matrix.image_name }}
    runs-on: ubuntu-latest
    # Per-variant concurrency lives on the job: workflow-level concurrency
    # cannot reference `matrix.*` (matrix context only exists inside jobs).
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ matrix.image_name }}
      cancel-in-progress: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - image_name: yaguarete_os
            variant_label: desktop
          - image_name: yaguarete_os-nvidia
            variant_label: nvidia
          - image_name: yaguarete_os-nvidia-open
            variant_label: nvidia-open
          - image_name: yaguarete_os-deck
            variant_label: deck

    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - name: Show disk space
        run: sudo df -h

      - name: Check if /mnt is there
        id: check_mnt
        run: |
          if sudo mountpoint /mnt; then
            echo "mnt_is_there=1" >>$GITHUB_OUTPUT
          else
            echo "mnt_is_there=0" >>$GITHUB_OUTPUT
          fi

      # Aggressive flags: free-disk-space keeps a lot of preinstalled stacks
      # by default. For deck the F43-vs-F44 lockstep duplicates several
      # paralel package sets during the rpm-ostree swap and the runner
      # ran out of disk. Pruning Android SDKs, .NET, Haskell, large APT
      # packages, prebuilt Docker images and swap recovers ~50 GB extra
      # vs the default ~30 GB.
      - name: Free Disk Space (Ubuntu)
        if: ${{ steps.check_mnt.outputs.mnt_is_there == '1' }}
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
        with:
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Mount Container Storage BTRFS Loopback
        run: |
          # /mnt on ubuntu-24.04 free-tier runners exposes ~75 GB. The
          # loopback file is sparse so we ask for the full available
          # window; podman never sees disk pressure until the underlying
          # /mnt itself fills, which only happens on the deck variant
          # because of the parallel F43/F44 libs during the swap.
          sudo truncate -s 75G /mnt/podman-storage.img
          sudo mkfs.btrfs -f /mnt/podman-storage.img
          sudo podman system reset --force
          sudo systemctl stop podman.service
          sudo mount -o compress-force=zstd:2 /mnt/podman-storage.img /var/lib/containers/storage
          sudo systemctl start podman.service

      - name: Report disk space after setup
        run: |
          df -h
          echo "::notice::Pre-build disk: $(df -h /mnt /var/lib/containers/storage | tail -2)"

      - name: Checkout Repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Get lower-case repo owner
        id: repo_owner_case
        run: echo "lowercase=${REPO_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Resolve channel + dated tag for ${{ matrix.image_name }}
        id: generate-tag
        shell: bash
        env:
          OWNER: ${{ steps.repo_owner_case.outputs.lowercase }}
          IMAGE: ${{ matrix.image_name }}
        run: |
          set -eo pipefail
          # Channel selection mirrors build.yml: testing → stable, anything
          # else (notably unstable) → unstable. ISO pulls the rolling pointer
          # and reuses the resolved dated tag for the filename so the artifact
          # carries the upstream Fedora/date trail.
          CHANNEL="unstable"
          if [[ "${{ github.ref_name }}" == "testing" ]]; then
            CHANNEL="stable"
          fi
          REGISTRY_IMAGE="ghcr.io/${OWNER}/${IMAGE}"
          DATED=""
          if UPSTREAM_TAG=$(skopeo inspect "docker://${REGISTRY_IMAGE}:${CHANNEL}" 2>/dev/null \
              | jq -er '.Labels["org.opencontainers.image.version"]'); then
            DATED="${CHANNEL}-${UPSTREAM_TAG}"
          fi
          FILENAME_TAG="${DATED:-$CHANNEL}"
          {
            echo "tag=${CHANNEL}"
            echo "dated=${DATED}"
            echo "filename_tag=${FILENAME_TAG}"
          } >> $GITHUB_OUTPUT

      - name: Build Payload Container
        run: |
          BASE_IMAGE="ghcr.io/${{ steps.repo_owner_case.outputs.lowercase }}/${{ matrix.image_name }}:${{ steps.generate-tag.outputs.tag }}"
          INSTALL_IMAGE_PAYLOAD="$BASE_IMAGE"
          FLATPAK_DIR_SHORTNAME="kde_flatpaks"

          sudo podman build \
            --cap-add sys_admin \
            --security-opt label=disable \
            --build-arg BASE_IMAGE="$BASE_IMAGE" \
            --build-arg INSTALL_IMAGE_PAYLOAD="$INSTALL_IMAGE_PAYLOAD" \
            --build-arg FLATPAK_DIR_SHORTNAME="$FLATPAK_DIR_SHORTNAME" \
            -t localhost/payload:latest installer/

      - name: Build ISO
        uses: Zeglius/titanoboa@revamp-pr
        id: build
        with:
          image-ref: localhost/payload:latest
          iso-dest: ${{ matrix.image_name }}-${{ steps.generate-tag.outputs.filename_tag }}-live-amd64.iso

      - name: Move ISO to Upload Directory
        id: upload-directory
        shell: bash
        run: |
          ISO_UPLOAD_DIR=${{ github.workspace }}/upload
          ISO_PATH=${{ steps.build.outputs.iso-dest }}
          ISO_NAME=$(basename ${ISO_PATH})

          mkdir -p ${ISO_UPLOAD_DIR}
          mv ${ISO_PATH} ${ISO_UPLOAD_DIR}
          (cd ${ISO_UPLOAD_DIR} && sha256sum ${ISO_NAME} | tee ${ISO_NAME}-CHECKSUM)
          echo "iso-upload-dir=${ISO_UPLOAD_DIR}" >> $GITHUB_OUTPUT

      - name: Attest Build Provenance
        id: attest
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4
        with:
          subject-path: ${{ steps.upload-directory.outputs.iso-upload-dir }}/*.iso

      - name: Generate ISO Metadata JSON
        env:
          ISO_UPLOAD_DIR: ${{ steps.upload-directory.outputs.iso-upload-dir }}
          ATTESTATION_URL: ${{ steps.attest.outputs.attestation-url }}
          VARIANT_LABEL: ${{ matrix.variant_label }}
        run: |
          for iso in ${ISO_UPLOAD_DIR}/*.iso; do
            iso_name=$(basename "$iso")
            jq -n \
              --arg iso "$iso_name" \
              --arg url "$ATTESTATION_URL" \
              --arg variant "$VARIANT_LABEL" \
              --arg date "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              '{iso: $iso, variant: $variant, attestation_url: $url, build_date: $date}' > "${iso}.json"
          done

      # ISOs always uploaded as Actions artifacts (login-required) for
      # smoke / audit. Plus: on push to `unstable`, the next step
      # republishes the same ISO to archive.org under a stable identifier
      # so the public landing has working download buttons that never
      # rotate URLs.
      - name: Upload ISO and Checksum to Job Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.image_name }}-${{ steps.generate-tag.outputs.filename_tag }}-iso
          path: ${{ steps.upload-directory.outputs.iso-upload-dir }}
          if-no-files-found: error
          retention-days: 7
          compression-level: 0
          overwrite: true

      # Public-facing distribution for the rolling unstable channel: upload
      # the same ISO + sidecars to archive.org under a fixed identifier
      # per variant. Filename is renamed to `<image>-unstable-amd64.iso`
      # so the public download URL stays stable across rebuilds.
      #
      # `ia upload --no-derive` skips archive.org's server-side derivative
      # generation (we ship a raw .iso, no point asking IA to transcode it).
      # `ia upload <identifier>` with the same identifier on subsequent
      # pushes overwrites the asset (no version explosion, no orphan items).
      #
      # Secrets configured by maintainer post-merge:
      #   gh secret set ARCHIVE_ORG_ACCESS_KEY --repo lobinuxsoft/yaguarete_os
      #   gh secret set ARCHIVE_ORG_SECRET_KEY --repo lobinuxsoft/yaguarete_os
      # (values from https://archive.org/account/s3.php)
      #
      # When the secrets are absent (e.g. fork builds), the step emits a
      # warning and exits 0 so the rest of the matrix stays green.
      - name: Publish rolling ISO to archive.org
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/unstable'
        env:
          IA_ACCESS_KEY: ${{ secrets.ARCHIVE_ORG_ACCESS_KEY }}
          IA_SECRET_KEY: ${{ secrets.ARCHIVE_ORG_SECRET_KEY }}
          ISO_UPLOAD_DIR: ${{ steps.upload-directory.outputs.iso-upload-dir }}
          IMAGE_NAME: ${{ matrix.image_name }}
          VARIANT_LABEL: ${{ matrix.variant_label }}
          UPSTREAM_TAG: ${{ steps.generate-tag.outputs.dated }}
        run: |
          set -eo pipefail
          if [ -z "${IA_ACCESS_KEY}" ] || [ -z "${IA_SECRET_KEY}" ]; then
            echo "::warning::archive.org keys not set (ARCHIVE_ORG_ACCESS_KEY / ARCHIVE_ORG_SECRET_KEY). Skipping upload for ${IMAGE_NAME}. See #111 for setup."
            exit 0
          fi

          # Internet Archive CLI; ships via pip. pipx keeps it isolated.
          python3 -m pip install --user --quiet --upgrade pip
          python3 -m pip install --user --quiet internetarchive
          # User-site bin onto PATH for this step.
          export PATH="$HOME/.local/bin:$PATH"

          # Single config file so `ia` finds the IAS3 credentials.
          mkdir -p "$HOME/.config/ia"
          cat > "$HOME/.config/ia/ia.ini" <<INI
          [s3]
          access = ${IA_ACCESS_KEY}
          secret = ${IA_SECRET_KEY}
          INI
          chmod 0600 "$HOME/.config/ia/ia.ini"

          # Rename to stable filename so the public download URL never moves.
          dated_iso=$(ls "${ISO_UPLOAD_DIR}"/*.iso | head -1)
          stable_iso="${ISO_UPLOAD_DIR}/${IMAGE_NAME}-unstable-amd64.iso"
          stable_sum="${stable_iso}-CHECKSUM"
          stable_meta="${stable_iso}.json"
          cp "${dated_iso}" "${stable_iso}"
          cp "${dated_iso}-CHECKSUM" "${stable_sum}"
          cp "${dated_iso}.json" "${stable_meta}"

          identifier="${IMAGE_NAME}-unstable-amd64"
          ia upload "${identifier}" \
            "${stable_iso}" "${stable_sum}" "${stable_meta}" \
            --no-derive \
            --metadata="title:YaguareteOS ${VARIANT_LABEL} — rolling unstable" \
            --metadata="creator:lobinuxsoft" \
            --metadata="description:YaguareteOS ${VARIANT_LABEL} variant, rolling unstable build (${UPSTREAM_TAG:-latest}). Bootable ISO, offline installable. Source: https://github.com/lobinuxsoft/yaguarete_os" \
            --metadata="subject:linux;distro;bazzite;yaguarete;gaming;kde;${VARIANT_LABEL}" \
            --metadata="licenseurl:https://www.apache.org/licenses/LICENSE-2.0" \
            --metadata="mediatype:software" \
            --metadata="collection:opensource"

          # Best-effort secret wipe from the runner FS. Process env is
          # discarded when the step ends regardless.
          rm -f "$HOME/.config/ia/ia.ini"
          echo "::notice::Published https://archive.org/details/${identifier}"
